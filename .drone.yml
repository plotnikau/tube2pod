kind: pipeline
name: default

workspace:
  base: /go
  path: src/github.com/plotnikau/tube2pod

steps:
- name: build
  image: golang
  commands:
  - go get -u github.com/golang/dep/cmd/dep
  - dep ensure
  - go build -o tube2pod
  - go test
